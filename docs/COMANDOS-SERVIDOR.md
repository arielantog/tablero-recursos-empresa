# Comandos para ejecutar el servidor

Ejecutar estos comandos dentro de la carpeta `backend`:

```bash
cd "c:\Users\ary90\OneDrive\Documentos\Visual Studio Code\portal-interno-empresa\backend"
npm install
npm run db:init
npm run dev
```

URL de acceso:

- `http://localhost:3000`
